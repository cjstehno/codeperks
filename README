CodePerks

This project consolidates my old codeperks projects. Since development has basically stopped for them, I figured it would 
make more sense to combine them into one project rather than having them spread across multiple repos.

The code here is stable and usable, though I am no longer actually using it anywhere.
