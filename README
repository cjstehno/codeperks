CodePerks

This project consolidates my old codeperks projects. Since development is basically stopped for them unless there is future interest I figured it would 
make more sense to combine them into one project.
